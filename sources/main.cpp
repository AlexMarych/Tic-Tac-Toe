#include <raylib.h>
#include <raygui.h>

int main() {
	
	const int screenWidth = 800;
	const int screenHeight = 450;
	InitWindow(screenWidth, screenHeight, "Angry Pirates");
	SetTargetFPS(60);             

	while (!WindowShouldClose())   
	{
		

	}
	
	CloseWindow();
	
	return 1;
}